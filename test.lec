
""" Test Comentary """

Class Test
( @value )
{
    @Test new {
        @self = (@super new);
        
        (@self isNotNull) ? {
            @lastNode = (@firstNode = @nil);
        };
        
        (@test isNotNull) ? "4" : { @hola; };
        
        {
            "test" length;
            -12.2e-2 abs;
            
            3 + 2;
        } eval;
        

        @self;
        
        (@self this:@s is:@e very:@j long:@k message:@self ?);
    }
    
    @self size {
        
        (@self isNotNull) ? {
            @size;
        };
        
        0;
    }
    
    @self , @secondList {
        6 !;
        
        6 factorial;
        
        @self;
    }
    
    @Test test {
        ("Hello world! " println) println; 
        (@String className)println;
    }
    
    
}

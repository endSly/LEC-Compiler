

Class List
| @firstNode @lastNode |
{
    @List new {
        @self = (@super new);
        
        (@self isNotNull) ? {
            @lastNode = (@firstNode = @nil);
        };
        
        @self;
    }
    
    
}
